const request = require('supertest');const expect = require('chai').expect;const knex = require('../db/knex');const app = require('../app');const fixtures = require('./fixtures');describe('API Allen Real Estates', () => {    before((done) => {        knex.migrate.latest()            .then(() => {                return knex.seed.run();            }).then(() => done());    });    // it('list all re records', (done) => {    //     request(app)    //         .get('/api/v1/real_estates')    //         .set('Accept', 'application/json')    //         .expect('Content-Type', 'application/json; charset=utf-8')    //         .expect(200, done);    // });    it('Lists all Records', (done) => {        request(app)            .get('/api/v1/real-estates')            .set('Accept', 'application/json')            .expect('Content-Type', /json/)            .expect(200)            .then((response) => {                expect(response.body).to.be.a('array');                expect(response.body).to.be.deep.equal(fixtures.realEstates);                done();            });    });    it('Returns record with id = 970', (done) => {        request(app)            .get('/api/v1/real-estates/970')            .set('Accept', 'application/json')            .expect('Content-Type', /json/)            .expect(200)            .then((response) => {                expect(response.body).to.be.a('object');                expect(response.body).to.be.deep.equal(fixtures.realEstate);                done();            });    });});